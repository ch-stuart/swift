<h1>
  <%= @page.title %>
</h1>

<p id="notice"><%= notice %></p>

<%= raw md @page.body %>

<% if @page.video_html.present? %>
  <%= raw @page.video_html %>
<% end %>

<%= content_for :footer do %>
  <%= render :partial => 'shared/footer' %>
<% end %>