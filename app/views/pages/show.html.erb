<!-- Rendered at <%= Time.now %> -->
<h1>
  <%= @page.title %>
</h1>

<p id="notice"><%= notice %></p>

<%= raw md @page.body %>

<% if @page.video_html.present? %>
  <%= raw @page.video_html %>
<% end %>

<%= render :partial => 'shared/slideshow' %>