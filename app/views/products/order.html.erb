<% unless @product.public? %>
  <p class="private" style="position: absolute; top: 0; left: 0; padding: 2px 6px; border: 1px solid rgba(0,0,0,.2);-webkit-box-shadow: 0 0 4px rgba(0,0,0,.3); border-top: 0;">
    private!
  </p>
<% end %>
<p id="notice">
  <%= notice %>
</p>
<h2>
  ## <%= @product.title %>
</h2>
<h3>
  ### About <%= @product.title %>
</h3>
<p>
  <%= @product.description %>
</p>
<p>
  $<%= @product.price %>
</p>
<h3>
  ### Specs
</h3>
<p>
  <%= @product.specs %>
</p>
<h3>
  ### Parts
</h3>
<div class="fields">
  <% @product.parts.each do |part| %>
    <div class="field">
      <p>
        <%= part.title %>
        <% if part.price.present? %>
          <%= "$#{part.price}" %>
        <% end %>
      </p>
      <p>
        <%= select_tag "#{part.title}_colors", options_from_collection_for_select(part.colors, "id", "title") %>
      </p>
    </div>
  <% end %>
</div>

<%= render :partial => 'shared/slideshow_html' %>

<h2>
  ### =>
</h2>
<p>
  <%= link_to "Pick Colors", :action => "colors", :id => @product %>
</p>